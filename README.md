# Chapter 8 Programming Projects (Java)

From this folder:

```bash
# Compile everything
javac -d out $(find src -name "*.java")

# Run PP 8.1 driver
java -cp out pp81.DivisorCalcDriver

# Run PP 8.4 driver
java -cp out pp84.EightQueensDriver
```
